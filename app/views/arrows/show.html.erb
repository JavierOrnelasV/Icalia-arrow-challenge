<div class='container'>
    <div class="card col-12">
        <div class="card-body">
            <h5 class="card-title"><%= @arrow.creator.name %></h6>
                <h6 class="card-subtitle text-muted mb-2"><%= @arrow.created_at.strftime('%d %b %Y') %></h6>
                <p class="card-text"><%= @arrow.reason %></p>
                <%= link_to "Back", arrows_path, :class => 'btn btn-dark' %>
        </div>
    </div>
</div>